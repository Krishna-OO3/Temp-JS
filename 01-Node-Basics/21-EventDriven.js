// Event Driven programming
/* const EventEmmiter = require('events')

const customEmmiter = new EventEmmiter()

// on -> listen for an event
// emit -> emit an event
customEmmiter.on('Response', (name, id) => {
  console.log(`Data recieved user:${name} with ID:${id}`);
})
customEmmiter.on('Response', () => {
  console.log('The data is pre-processed');
})

// customEmmiter.emit('Response')
// the emit should be properly kept in place

// passing argumets when emmitting
customEmmiter.emit('Response','John', 24) */